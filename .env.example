# Copy to .env and replace with a local-only development password.
VARYS_POSTGRES_PASSWORD=replace-with-a-local-development-password
VARYS_SESSION_SECRET=replace-with-a-long-random-local-development-secret

# Required only when starting the optional cloudflared Compose profile.
# CLOUDFLARE_TUNNEL_TOKEN=
